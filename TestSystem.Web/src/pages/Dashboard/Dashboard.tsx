import React from 'react';

const Dashboard: React.FC = () => {
    return (
        <div
            className={`min-h-screen bg-gray-100 dark:bg-gray-900 text-gray-900 dark:text-gray-100 transition-colors duration-500`}>
        </div>
    );
};

export default Dashboard;
